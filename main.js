// Tic Tac Toe with Colors instead of Xs and Os

// GLOBAL CONSTANTS
const NUM_ROWS = 3;
const NUM_COLS = 3;

// Ideas for what to add are at the bottom of this file (only if you have no ideas)
// Some functions you should add are at the bottom of this file

// Create an array to represent a grid
let grid = createGridArray();

// Create divs to model the grid array
createDivGrid(grid);

// In the grid, 1 represents X, -1 represents O, and 0 represents an empty tile






























































/* End of file */
